<%= search_form_for(@query, url: search_path, method: :get, class:'d-flex') do |f| %>
<%# following establishes where the search takes place; i.e., what tables and what fields in these tables. %>
    <%= f.search_field :title_or_body_or_user_email_or_user_name_i_cont_any,
    placeholder: "Search...",
    class: 'form-control me-2' %>
    <%= f.submit "Search", class: 'btn btn-outline-success' %>
<% end %>
